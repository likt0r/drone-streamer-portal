services:
  nginx:
    image: nginx:alpine
    ports:
      - "5000:80"
    volumes:
      - ./nginx/dev.conf:/etc/nginx/conf.d/default.conf:ro
    extra_hosts:
      # Allow nginx inside Docker to reach services on the host machine
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
